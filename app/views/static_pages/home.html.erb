<div class="center">
    <h1>Home Page</h1>
    <h3>Dunno what this app is called yet.</h3>
    <p>Seeing if I can build a relatively functional social network using Ruby on Rails.</p>
    
    <% if !user_signed_in? %>
        <%= link_to 'Register', new_user_registration_path, :class => "btn" %>
        <%= link_to 'Sign In', new_user_session_path, :class => "btn" %>
    <% else %>
        <%= link_to 'Posts Feed', posts_path, :class => "btn" %>
        <%= link_to 'My Profile', profile_path, :class => "btn" %>
    <% end %>
</div>